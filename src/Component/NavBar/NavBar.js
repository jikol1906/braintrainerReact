import React from 'react';
import './NavBar.css'
import Logo from "./Logo/Logo";

const NavBar = () => {
    return (
        <header className="NavBar">
            <Logo/>
            <nav>

            </nav>
        </header>
    );
};

export default NavBar;
